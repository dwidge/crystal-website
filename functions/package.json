{
	"name": "template-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy index.ts",
		"dev": "wrangler dev index.ts",
		"test": "vitest"
	},
	"devDependencies": {
		"vitest": "^1.6.0",
		"wrangler": "^3.0.0"
	},
	"dependencies": {
		"itty-router": "^5.0.17"
	}
}
